const setRegions = jest.fn();

const setCategories = jest.fn();

const setRestaurants = jest.fn();

const checkRegion = jest.fn();

const checkCategory = jest.fn();

const updateRegions = jest.fn();

const getRegions = jest.fn();

const getCategories = jest.fn();

const getRestaurants = jest.fn();

const updateCategories = jest.fn();

export {
  setRegions, setCategories, setRestaurants, checkRegion, checkCategory,
  updateRegions, updateCategories, getRegions, getCategories, getRestaurants,
};
