import { fetchCategories, fetchRegions, fetchRestaurant } from "./api"

test('', () => {
  // expect(fetchCategories()).not.toBeNull()
  // expect(fetchRegions()).not.toBeNull()
  // expect(fetchRestaurant(1, 1)).not.toBeNull()
})