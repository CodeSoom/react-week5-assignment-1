import { regions } from './regions';
import { categories } from './categories';

export default {
  regions,
  categories,
  restaurants: [{ id: 1, name: '홍콩반점' }],
  selectedRegion: '서울',
  selectedCategory: '1',
};
